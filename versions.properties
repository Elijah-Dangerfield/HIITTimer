# Android version metadata.
#   applicationId — canonical; read on every build.
#   versionName   — managed by release-please (see markers below).
#                   Also mirrored into apps/ios/Configuration/Config.xcconfig.
# versionCode / buildNumber / releaseChannel are injected by CI via the env vars
# VERSION_CODE_OVERRIDE, BUILD_NUMBER_OVERRIDE, RELEASE_CHANNEL_OVERRIDE.
# Local dev builds fall back to defaults in build-logic/util/Versioning.kt.
applicationId=com.dangerfield.hiittimer.HIITTimer
# x-release-please-start-version
versionName=0.1.0
# x-release-please-end
